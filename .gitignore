.venv/
__pycache__/
*.pyc
.DS_Store
TRAIN_SET/
TRAIN_SET.zip
cache/
.ipynb_checkpoints/

# Track reports (markdown, small CSVs) and models (small .json / .npz classifiers)
# Only ignore large generated artifacts inside them:
reports/pitch/*.png
reports/samples/*.png
reports/raw_samples/*.png
models/**/train_predictions.csv

# Pitch deck depends on these — unignore explicitly so html stays usable on clone
!reports/pitch/02_class_morphology_grid.jpg
!reports/pitch/04_confusion_matrix.png

# Per-agent transcript dumps from internal task system (not portable)
.claude/

# Hackathon-submission scratch (delete after copy-pasting into form)
_submission_draft.md
SCRIPTS.md

# Gallery: ignore generated assets but keep deck screenshots + DevPost thumbnail
_gallery/*
!_gallery/slide_*.png
!_gallery/thumbnail_devpost.jpg
